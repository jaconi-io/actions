# jaconi GitHub Actions

## setup-flux-envsubst

Setup [flux-envsubst](https://github.com/jaconi-io/flux-envsubst).

```
- uses: jaconi-io/actions/setup-flux-envsubst
  with:
    version: v4.0.32
```

You can use `latest` or omit the version to use the latest release.
